import { GreenbgDirective } from './greenbg.directive';

describe('GreenbgDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenbgDirective();
    expect(directive).toBeTruthy();
  });
});
