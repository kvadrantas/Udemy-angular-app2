import { RedbgDirective } from './redbg.directive';

describe('RedbgDirective', () => {
  it('should create an instance', () => {
    const directive = new RedbgDirective();
    expect(directive).toBeTruthy();
  });
});
