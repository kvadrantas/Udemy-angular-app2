import { BluebgDirective } from './bluebg.directive';

describe('BluebgDirective', () => {
  it('should create an instance', () => {
    const directive = new BluebgDirective();
    expect(directive).toBeTruthy();
  });
});
