import { ShowOddDirective } from './show-odd.directive';

describe('ShowOddDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOddDirective();
    expect(directive).toBeTruthy();
  });
});
